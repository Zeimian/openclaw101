# Downloads 目录说明

请将以下两个安装文件放入此目录：

1. `ClawPanel_0.10.0_x64-setup.exe` — Windows 安装包
2. `OpenClawInstaller-exe-.zip` — ZIP 压缩包

放置后，Day 2 页面的下载链接将可以正常工作。

> 注意：这两个二进制文件较大，建议不要提交到 git。可以在 `.gitignore` 中排除 `*.exe` 和 `*.zip`。
